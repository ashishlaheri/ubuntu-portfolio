apiVersion: v1
kind: Service
metadata:
  name: portfolio-service
spec:
  # Internal load balancer for the pods
  type: ClusterIP
  selector:
    app: portfolio
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
