# Dependencies & build output (rebuilt inside Docker)
node_modules
dist

# Version control
.git
.gitignore

# Environment files (never bake secrets into images)
.env
.env.*

# Editor & OS noise
.vscode
.idea
.DS_Store
Thumbs.db

# CI / DevOps config not needed at runtime
k8s
output.txt
*.md
